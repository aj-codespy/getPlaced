%-------------------------
% Resume in Latex
% Classic Template
%------------------------

\documentclass[letterpaper,11pt]{article}

\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage[usenames,dvipsnames]{color}
\usepackage{enumitem}
\usepackage[pdftex]{hyperref}
\usepackage{fancyhdr}
\usepackage{mathptmx} % Times New Roman

\pagestyle{fancy}
\fancyhf{} 
\fancyfoot{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Margins
\addtolength{\oddsidemargin}{-0.75in}
\addtolength{\evensidemargin}{-0.75in}
\addtolength{\textwidth}{1.5in}
\addtolength{\topmargin}{-.75in}
\addtolength{\textheight}{1.5in}

\urlstyle{same}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

% Section Formatting
\titleformat{\section}{
  \vspace{-12pt}\scshape\raggedright\large\bfseries
}{}{0em}{}[\color{black}\titlerule \vspace{-6pt}]

\begin{document}

% --- HEADER ---
\begin{center}
    {\Huge \bfseries \uppercase{\VAR{data.personalInfo.fullName}}} \\ \vspace{5pt}
    \small
    \BLOCK{set contact = []}
    \BLOCK{if data.personalInfo.email}\BLOCK{set _ = contact.append(data.personalInfo.email)}\BLOCK{endif}
    \BLOCK{if data.personalInfo.phone}\BLOCK{set _ = contact.append(data.personalInfo.phone)}\BLOCK{endif}
    \BLOCK{if data.personalInfo.location}\BLOCK{set _ = contact.append(data.personalInfo.location)}\BLOCK{endif}
    \BLOCK{if contact}
      \BLOCK{for c in contact}\VAR{c}\BLOCK{if not loop.last} \quad $|$ \quad \BLOCK{endif}\BLOCK{endfor} \\
    \BLOCK{endif}
    \BLOCK{if data.personalInfo.displayLinks}
      \BLOCK{for link in data.personalInfo.displayLinks}\href{\VAR{link.url}}{\VAR{link.label}}\BLOCK{if not loop.last} \quad $|$ \quad \BLOCK{endif}\BLOCK{endfor}
    \BLOCK{endif}
    \vspace{4pt}
    \hrule
\end{center}

\vspace{0.1cm}

% --- SUMMARY ---
\BLOCK{if data.personalInfo.summary}
\section{Summary}
\small{\VAR{data.personalInfo.summary}}
\vspace{0.15cm}
\BLOCK{endif}

% --- EXPERIENCE ---
\BLOCK{if data.experience}
\section{Experience}
\begin{itemize}[leftmargin=0in, label={}]
    \BLOCK{for exp in data.experience}
    \item
      \begin{tabular*}{0.98\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{\VAR{exp.company}} & \VAR{exp.startDate} -- \VAR{exp.endDate} \\
        \textit{\small \VAR{exp.role}} & 
      \end{tabular*}
      \begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in exp.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
      \end{itemize}
      \vspace{2pt}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- EDUCATION ---
\BLOCK{if data.education}
\section{Education}
\begin{itemize}[leftmargin=0in, label={}]
    \BLOCK{for edu in data.education}
    \item
      \begin{tabular*}{0.98\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{\VAR{edu.institution}} & \VAR{edu.startDate} -- \VAR{edu.endDate} \\
        \textit{\small \VAR{edu.degree}} & \BLOCK{if edu.score} \small GPA: \VAR{edu.score} \BLOCK{endif}
      \end{tabular*}
      \vspace{2pt}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- SKILLS ---
\BLOCK{if data.skills}
\section{Skills}
\begin{itemize}[leftmargin=0in, label={}]
    \small{\item{
     \BLOCK{for skill in data.skills} \VAR{skill}\BLOCK{if not loop.last} $\bullet$ \BLOCK{endif} \BLOCK{endfor}
    }}
\end{itemize}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Projects}
\begin{itemize}[leftmargin=0in, label={}]
    \BLOCK{for proj in data.projects}
    \item
      \begin{tabular*}{0.98\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{\VAR{proj.title}} & \textit{\VAR{proj.role}} \\
      \end{tabular*}
      \BLOCK{if proj.description}
      \begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \item \small \VAR{proj.description}
      \end{itemize}
      \BLOCK{endif}
      \BLOCK{if proj.bullets}
      \begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
          \BLOCK{for bullet in proj.bullets}
          \item \small \VAR{bullet}
          \BLOCK{endfor}
      \end{itemize}
      \BLOCK{endif}
      \BLOCK{if proj.techStack}
      \small{\textbf{Technologies:} \BLOCK{for tech in proj.techStack}\VAR{tech}\BLOCK{if not loop.last}, \BLOCK{endif}\BLOCK{endfor}}
      \BLOCK{endif}
      \vspace{2pt}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Achievements}
\begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for ach in data.achievements}
    \item \small \VAR{ach}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for cert in data.certifications}
    \item \small \VAR{cert}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- PUBLICATIONS ---
\BLOCK{if data.publications}
\section{Publications}
\begin{itemize}[leftmargin=0in, label={}]
    \BLOCK{for pub in data.publications}
    \item \small \textbf{\VAR{pub.title}}. \textit{\VAR{pub.journal}}. \VAR{pub.date}.
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

\end{document}
