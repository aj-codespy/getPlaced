%-------------------------
% Resume in Latex
% Elegant Template - Serif, Accent Color, Refined
%------------------------

\documentclass[11pt,a4paper]{article}

\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{xurl}

% Color - Burgundy/Dark Red
\definecolor{accent}{RGB}{102, 0, 0}
\definecolor{textgray}{RGB}{60, 60, 60}
\definecolor{ACCENT}{RGB}{102, 0, 0}
\definecolor{TEXTGRAY}{RGB}{60, 60, 60}

% Margins
\geometry{left=1.2cm, right=1.2cm, top=1.2cm, bottom=1.2cm}

% Font (Default Serif is good for Elegant)

\pagestyle{empty}

\hypersetup{
    colorlinks=true,
    linkcolor=accent,
    urlcolor=accent,
}
\urlstyle{same}
\setlength{\emergencystretch}{2em}

% Section Headers: Center, Small Caps, Accent Color
\titleformat{\section}
  {\centering\normalfont\Large\scshape\color{accent}}
  {}
  {0em}
  {}
  [\vspace{-4pt}\hrule]

\titlespacing{\section}{0pt}{10pt}{6pt}

\begin{document}

% --- HEADER ---
\begin{center}
    {\Huge \scshape \color{accent} \VAR{data.personalInfo.fullName}} \\ \vspace{4pt}
    {\small \color{textgray}
      \parbox{0.95\textwidth}{\centering
      \BLOCK{set contact = []}
      \BLOCK{if data.personalInfo.location}\BLOCK{set _ = contact.append(data.personalInfo.location)}\BLOCK{endif}
      \BLOCK{if data.personalInfo.phone}\BLOCK{set _ = contact.append(data.personalInfo.phone)}\BLOCK{endif}
      \BLOCK{if data.personalInfo.email}\BLOCK{set _ = contact.append('\\nolinkurl{' ~ data.personalInfo.email ~ '}')}\BLOCK{endif}
      \BLOCK{for c in contact}\VAR{c}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor}
      }
    } \\ \vspace{4pt}
    \BLOCK{if data.personalInfo.displayLinks}
    {\small
      \parbox{0.95\textwidth}{\centering
      \BLOCK{for link in data.personalInfo.displayLinks}\href{\VAR{link.url}}{\VAR{link.label}}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor}
      }
    }
    \BLOCK{endif}
\end{center}

\vspace{4pt}

% --- SUMMARY ---
\BLOCK{if data.personalInfo.summary}
\begin{center}
    \parbox{0.9\textwidth}{
        \centering \small \color{textgray} \textit{\VAR{data.personalInfo.summary}}
    }
\end{center}
\vspace{6pt}
\BLOCK{endif}

% --- EXPERIENCE ---
\BLOCK{if data.experience}
\section{Professional Experience}
\BLOCK{for exp in data.experience}
    \noindent
    \BLOCK{if exp.role or exp.dateRange}
    \textbf{\large \VAR{exp.role}}\BLOCK{if exp.dateRange} \hfill {\small \VAR{exp.dateRange}}\BLOCK{endif} \\
    \BLOCK{endif}
    {\small \color{accent} \uppercase{\VAR{exp.company}}} \hfill {\small \color{textgray} \VAR{exp.location}}
    \vspace{4pt}
    \begin{itemize}[leftmargin=*, label={\color{accent}\small\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in exp.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- EDUCATION ---
\BLOCK{if data.education}
\section{Education}
\BLOCK{for edu in data.education}
    \noindent
    \textbf{\VAR{edu.institution}}\BLOCK{if edu.dateRange} \hfill \VAR{edu.dateRange}\BLOCK{endif} \\
    {\small \textit{\VAR{edu.degree}}}
    \BLOCK{if edu.score} \hfill {\small GPA: \VAR{edu.score}} \BLOCK{endif}
    \vspace{3pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Key Projects}
\begin{itemize}[leftmargin=*, label={}, itemsep=3pt, parsep=0pt, topsep=2pt]
\BLOCK{for proj in data.projects}
    \item 
    \textbf{\VAR{proj.title}} \BLOCK{if proj.link} \href{\VAR{proj.link}}{\footnotesize [Link]} \BLOCK{endif}
    \BLOCK{if proj.techStack}\quad \textemdash \quad {\small \color{textgray} \textit{\VAR{proj.techStack|join(', ')}}}\BLOCK{endif} \\
    \BLOCK{if proj.description}{\small \VAR{proj.description}}\BLOCK{endif}
    \BLOCK{if proj.bullets}
    \begin{itemize}[leftmargin=0.5em, label={\color{accent}\small\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in proj.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \BLOCK{endif}
\BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- SKILLS ---
\BLOCK{if data.skills}
\section{Expertise}
\begin{center}
    \small
    \VAR{data.skills|join('  \textbullet  ')}
\end{center}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Achievements}
\begin{itemize}[leftmargin=*, label={\color{accent}\small\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for ach in data.achievements}
    \item \small \VAR{ach}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=*, label={\color{accent}\small\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for cert in data.certifications}
    \item \small \VAR{cert}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- PUBLICATIONS ---
\BLOCK{if data.publications}
\section{Publications}
\begin{enumerate}[leftmargin=*, label={[\arabic*]}]
    \BLOCK{for pub in data.publications}
    \item \small \textbf{\VAR{pub.title}}. \textit{\VAR{pub.journal}}. \VAR{pub.date}.
    \BLOCK{endfor}
\end{enumerate}
\BLOCK{endif}

\end{document}
