%-------------------------
% Resume in Latex
% Impact Template - Consulting/Finance Style, Very Clean, Dense
%------------------------

\documentclass[10pt,letterpaper]{article}

\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}

% Margins (Std Consulting margins)
\geometry{left=0.5in, right=0.5in, top=0.5in, bottom=0.5in}

\pagestyle{empty}

% Font: Times-like (Standard for business)
% Default Latex font is fine, or use mathptmx for Times
\usepackage{mathptmx}

% Section Titles: Small Caps, Bold, Underlined
\titleformat{\section}
  {\normalfont\large\bfseries\uppercase} 
  {} 
  {0em} 
  {} 
  [\titlerule]

\titlespacing{\section}{0pt}{8pt}{4pt}

\begin{document}

% --- HEADER ---
\begin{center}
    {\LARGE \textbf{\VAR{data.personalInfo.fullName}}} \\ \vspace{6pt}
    \BLOCK{set contact = []}
    \BLOCK{if data.personalInfo.location}\BLOCK{set _ = contact.append(data.personalInfo.location)}\BLOCK{endif}
    \BLOCK{if data.personalInfo.phone}\BLOCK{set _ = contact.append(data.personalInfo.phone)}\BLOCK{endif}
    \BLOCK{if data.personalInfo.email}\BLOCK{set _ = contact.append(data.personalInfo.email)}\BLOCK{endif}
    \BLOCK{for c in contact}\VAR{c}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor} \\
    \vspace{4pt}
    \BLOCK{if data.personalInfo.displayLinks}
      \BLOCK{for link in data.personalInfo.displayLinks}\href{\VAR{link.url}}{\VAR{link.label}}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor}
    \BLOCK{endif}
\end{center}

\vspace{4pt}

% --- SUMMARY ---
\BLOCK{if data.personalInfo.summary}
\noindent {\small \VAR{data.personalInfo.summary}}
\vspace{4pt}
\BLOCK{endif}

% --- EXPERIENCE (Consulting prioritizes Experience/Leadership) ---
\BLOCK{if data.experience}
\section{Professional Experience}
\BLOCK{for exp in data.experience}
    \noindent
    \textbf{\VAR{exp.company}} \hfill \VAR{exp.location} \\
    \textit{\VAR{exp.role}} \hfill \VAR{exp.startDate} -- \VAR{exp.endDate}
    \begin{itemize}[leftmargin=*, label={--}, itemsep=0pt]
        \BLOCK{for bullet in exp.bullets}
        \item \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Leadership \& Projects}
\BLOCK{for proj in data.projects}
    \noindent
    \textbf{\VAR{proj.title}} \BLOCK{if proj.link} \href{\VAR{proj.link}}{[Link]} \BLOCK{endif} \hfill \textit{\VAR{proj.techStack|join(', ')}} \\
    \small \VAR{proj.description}
    \BLOCK{if proj.bullets}
    \begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in proj.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \BLOCK{endif}
    \vspace{3pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- EDUCATION ---
\BLOCK{if data.education}
\section{Education}
\BLOCK{for edu in data.education}
    \noindent
    \textbf{\VAR{edu.institution}} \hfill \VAR{edu.startDate} -- \VAR{edu.endDate} \\
    \VAR{edu.degree} \hfill \BLOCK{if edu.score} GPA: \VAR{edu.score} \BLOCK{endif}
    \vspace{3pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PUBLICATIONS ---
\BLOCK{if data.publications}
\section{Selected Publications}
\begin{itemize}[leftmargin=*, label={--}, itemsep=1pt, parsep=0pt, topsep=2pt]
\BLOCK{for pub in data.publications}
    \item \VAR{pub.title}, \textit{\VAR{pub.journal}} (\VAR{pub.date})
\BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- SKILLS & ADDITIONAL ---
\BLOCK{if data.skills}
\section{Skills \& Interests}
\noindent
\VAR{data.skills|join(', ')}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Achievements}
\begin{itemize}[leftmargin=*, label={--}, itemsep=0pt]
    \BLOCK{for ach in data.achievements}
    \item \VAR{ach}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=*, label={--}, itemsep=0pt]
    \BLOCK{for cert in data.certifications}
    \item \VAR{cert}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

\end{document}
