%-------------------------
% Resume in Latex
% Ivy Template - Academic, Serif, Prestige, Research-Focused
%------------------------

\documentclass[11pt,letterpaper]{article}

\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{xurl}

% Margins
\geometry{left=0.5in, right=0.5in, top=0.5in, bottom=0.5in}

\pagestyle{empty}
\urlstyle{same}
\setlength{\emergencystretch}{3em}

% Section Titles: Small Caps, Centered, with Rule
\titleformat{\section}
  {\normalfont\Large\scshape\centering} 
  {} 
  {0em} 
  {} 
  [\vspace{-4pt}\hrule]

\titlespacing{\section}{0pt}{10pt}{5pt}

\begin{document}

% --- HEADER ---
\begin{center}
    {\huge \textbf{\VAR{data.personalInfo.fullName}}} \\ \vspace{6pt}
    {\small \parbox{0.95\textwidth}{\centering
    \BLOCK{if data.personalInfo.location}\VAR{data.personalInfo.location} \\ \BLOCK{endif}
    \BLOCK{set contact = []}
    \BLOCK{if data.personalInfo.email}\BLOCK{set _ = contact.append('\\nolinkurl{' ~ data.personalInfo.email ~ '}')}\BLOCK{endif}
    \BLOCK{if data.personalInfo.phone}\BLOCK{set _ = contact.append(data.personalInfo.phone)}\BLOCK{endif}
    \BLOCK{for c in contact}\VAR{c}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor} \\
    \BLOCK{if data.personalInfo.displayLinks}
      \BLOCK{for link in data.personalInfo.displayLinks}\href{\VAR{link.url}}{\VAR{link.label}}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor}
    \BLOCK{endif}
    }}
\end{center}

\vspace{4pt}

% --- SUMMARY ---
\BLOCK{if data.personalInfo.summary}
\noindent {\small \VAR{data.personalInfo.summary}}
\vspace{4pt}
\BLOCK{endif}

% --- EDUCATION (Top for Academic) ---
\BLOCK{if data.education}
\section{Education}
\BLOCK{for edu in data.education}
    \noindent
    \textbf{\VAR{edu.institution}} \hfill \VAR{edu.endDate} \\
    {\textit{\VAR{edu.degree}}} \\
    \BLOCK{if edu.score} GPA: \VAR{edu.score} \\ \BLOCK{endif}
    \BLOCK{if edu.coursework} {\small \textit{Relevant Coursework: \VAR{edu.coursework}}} \\ \BLOCK{endif}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PUBLICATIONS (Priority for Research) ---
\BLOCK{if data.publications}
\section{Publications}
\begin{enumerate}[leftmargin=*, label={[\arabic*]}]
\BLOCK{for pub in data.publications}
    \item \textbf{\VAR{pub.title}}. \textit{\VAR{pub.journal}}. \VAR{pub.date}.
\BLOCK{endfor}
\end{enumerate}
\BLOCK{endif}

% --- RESEARCH / EXPERIENCE ---
\BLOCK{if data.experience}
\section{Experience}
\BLOCK{for exp in data.experience}
    \noindent
    \textbf{\VAR{exp.company}}\BLOCK{if exp.dateRange} \hfill \VAR{exp.dateRange}\BLOCK{endif} \\
    \BLOCK{if exp.role}\textit{\VAR{exp.role}}\BLOCK{endif}
    \begin{itemize}[leftmargin=0.45em, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in exp.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Projects}
\BLOCK{for proj in data.projects}
    \noindent
    \textbf{\VAR{proj.title}} \BLOCK{if proj.link} \href{\VAR{proj.link}}{[Link]} \BLOCK{endif} \\
    \BLOCK{if proj.techStack}{\small \textit{\VAR{proj.techStack|join(', ')}}}\BLOCK{endif}
    \BLOCK{if proj.description}
    \begin{itemize}[leftmargin=0.45em, itemsep=1pt]
        \item \small \VAR{proj.description}
    \end{itemize}
    \BLOCK{endif}
    \BLOCK{if proj.bullets}
    \begin{itemize}[leftmargin=0.45em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in proj.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \BLOCK{endif}
    \vspace{3pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- SKILLS ---
\BLOCK{if data.skills}
\section{Skills}
\begin{center}
    \small
    \VAR{data.skills|join(' \textbullet{} ')}
\end{center}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Honors \& Awards}
\begin{itemize}[leftmargin=0.45em, itemsep=1pt, parsep=0pt, topsep=2pt]
\BLOCK{for ach in data.achievements}
    \item \small \VAR{ach}
\BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=0.45em, itemsep=1pt, parsep=0pt, topsep=2pt]
\BLOCK{for cert in data.certifications}
    \item \small \VAR{cert}
\BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

\end{document}
