%-------------------------
% Resume in Latex
% Compact Template - High Density, Single Column, Efficient
%------------------------

\documentclass[10pt,letterpaper]{article}

\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}

% Margins (Tight)
\geometry{left=0.5in, right=0.5in, top=0.5in, bottom=0.5in}

\pagestyle{empty}

% Colors
\definecolor{linkcolor}{RGB}{0, 79, 144}

\hypersetup{
    colorlinks=true,
    linkcolor=linkcolor,
    urlcolor=linkcolor,
}

% Section Formatting (Small, Uppercase, Line)
\titleformat{\section}
  {\large\bfseries\uppercase} 
  {} 
  {0em} 
  {} 
  [\titlerule]

\titlespacing{\section}{0pt}{10pt}{5pt}

\begin{document}

% --- HEADER ---
\begin{center}
    {\huge \textbf{\VAR{data.personalInfo.fullName}}} \\ \vspace{4pt}
    \small 
    \VAR{data.personalInfo.location} \quad | \quad 
    \href{mailto:\VAR{data.personalInfo.email}}{\VAR{data.personalInfo.email}} \quad | \quad 
    \VAR{data.personalInfo.phone} \\
    \BLOCK{if data.personalInfo.linkedin} \href{\VAR{data.personalInfo.linkedin}}{LinkedIn} \quad | \quad \BLOCK{endif}
    \BLOCK{if data.personalInfo.github} \href{\VAR{data.personalInfo.github}}{GitHub} \quad | \quad \BLOCK{endif}
    \BLOCK{if data.personalInfo.portfolio} \href{\VAR{data.personalInfo.portfolio}}{Portfolio} \BLOCK{endif}
\end{center}

% --- SUMMARY ---
\BLOCK{if data.personalInfo.summary}
\section{Summary}
\small \VAR{data.personalInfo.summary}
\BLOCK{endif}

% --- SKILLS (Top for technical roles) ---
\BLOCK{if data.skills}
\section{Skills}
\vspace{1pt}
\small
\noindent \textbf{Core Competencies:} \VAR{data.skills|join(', ')}
\BLOCK{endif}


% --- EXPERIENCE ---
\BLOCK{if data.experience}
\section{Experience}
\BLOCK{for exp in data.experience}
    \noindent
    \textbf{\VAR{exp.role}} \hfill \textbf{\VAR{exp.startDate} -- \VAR{exp.endDate}} \\
    \textit{\VAR{exp.company}} \hfill \textit{\VAR{exp.location}}
    \vspace{-2pt}
    \begin{itemize}[leftmargin=15pt, itemsep=1pt, parsep=0pt]
        \BLOCK{for bullet in exp.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Projects}
\BLOCK{for proj in data.projects}
    \noindent
    \textbf{\VAR{proj.title}} \BLOCK{if proj.link} \href{\VAR{proj.link}}{[Link]} \BLOCK{endif} \hfill \textit{\VAR{proj.techStack|join(', ')}} \\
    \small \VAR{proj.description}
    \BLOCK{if proj.bullets}
    \begin{itemize}[leftmargin=1.5em]
        \BLOCK{for bullet in proj.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \BLOCK{endif}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- EDUCATION ---
\BLOCK{if data.education}
\section{Education}
\BLOCK{for edu in data.education}
    \noindent
    \textbf{\VAR{edu.institution}} \hfill \VAR{edu.startDate} -- \VAR{edu.endDate} \\
    \textit{\VAR{edu.degree}} \hfill \BLOCK{if edu.score} GPA: \VAR{edu.score} \BLOCK{endif}
    \BLOCK{if edu.coursework} \\ \footnotesize Relevant Coursework: \VAR{edu.coursework} \BLOCK{endif}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Achievements}
\begin{itemize}[leftmargin=15pt, itemsep=1pt]
\BLOCK{for ach in data.achievements}
    \item \small \VAR{ach}
\BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=15pt, itemsep=1pt]
\BLOCK{for cert in data.certifications}
    \item \small \VAR{cert}
\BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- PUBLICATIONS ---
\BLOCK{if data.publications}
\section{Publications}
\begin{enumerate}[leftmargin=*, label={[\arabic*]}, itemsep=1pt]
\BLOCK{for pub in data.publications}
    \item \small \VAR{pub.title}. \textit{\VAR{pub.journal}}. \VAR{pub.date}.
\BLOCK{endfor}
\end{enumerate}
\BLOCK{endif}

\end{document}
