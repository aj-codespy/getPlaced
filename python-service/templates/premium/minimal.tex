%-------------------------
% Resume in Latex
% Minimal Template - Clean, Swiss Style, Whitespace heavy
%------------------------

\documentclass[11pt,a4paper]{article}

\usepackage[scaled]{helvet}
\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{xurl}

% Font
\renewcommand{\familydefault}{\sfdefault}

% Colors
\definecolor{black}{RGB}{0,0,0}
\definecolor{darkgray}{RGB}{60,60,60}

% Margins
\geometry{left=1cm, right=1cm, top=1cm, bottom=1cm}

% Remove page numbers
\pagestyle{empty}

% Links
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=black,
    urlcolor=black,
}
\urlstyle{same}
\setlength{\emergencystretch}{3em}

% Section Titles: Large, Bold, No Line
\titleformat{\section}
  {\huge\bfseries\color{black}} % Format
  {} % Label
  {0em} % Sep
  {} % Before
  [\vspace{-6pt}] % After

\titlespacing{\section}{0pt}{12pt}{6pt}

\begin{document}

% --- HEADER ---
\noindent
{\parbox{\textwidth}{\raggedright\fontsize{34}{40}\selectfont \textbf{\uppercase{\VAR{data.personalInfo.fullName}}}}} \\
\vspace{8pt}
\BLOCK{if data.personalInfo.summary}
{\Large \color{darkgray} \VAR{data.personalInfo.summary}} \\
\BLOCK{endif}
\vspace{6pt}

\noindent
\color{darkgray}
{\small
\parbox{\textwidth}{
\raggedright
\BLOCK{set contact = []}
\BLOCK{if data.personalInfo.location}\BLOCK{set _ = contact.append(data.personalInfo.location)}\BLOCK{endif}
\BLOCK{if data.personalInfo.email}\BLOCK{set _ = contact.append('\\nolinkurl{' ~ data.personalInfo.email ~ '}')}\BLOCK{endif}
\BLOCK{if data.personalInfo.phone}\BLOCK{set _ = contact.append(data.personalInfo.phone)}\BLOCK{endif}
\BLOCK{for c in contact}\VAR{c}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor}
\BLOCK{if data.personalInfo.displayLinks}
  \BLOCK{if contact} \par\vspace{-2pt} \BLOCK{endif}
  \BLOCK{for link in data.personalInfo.displayLinks}\href{\VAR{link.url}}{\VAR{link.label}}\BLOCK{if not loop.last} \quad \textbullet \quad \BLOCK{endif}\BLOCK{endfor}
\BLOCK{endif}
}
}
\vspace{10pt}

% --- SUMMARY (rendered in header above) ---

% --- EXPERIENCE ---
\BLOCK{if data.experience}
\section{Experience}
\BLOCK{for exp in data.experience}
    \noindent
    \textbf{\Large \VAR{exp.role}} \hfill {\color{darkgray} \VAR{exp.startDate} -- \VAR{exp.endDate}} \\
    {\large \color{darkgray} \VAR{exp.company}, \VAR{exp.location}}
    \vspace{4pt}
    \begin{itemize}[leftmargin=*, label={}, itemsep=4pt]
        \BLOCK{for bullet in exp.bullets}
        \item \large \color{darkgray} \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \vspace{6pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Projects}
\BLOCK{for proj in data.projects}
    \noindent
    \textbf{\Large \VAR{proj.title}}
    \BLOCK{if proj.link} \href{\VAR{proj.link}}{\color{blue} \textit{[Link]}} \BLOCK{endif}
    \BLOCK{if proj.role} \\[-1pt] {\small \color{darkgray} \VAR{proj.role}} \BLOCK{endif} \\
    {\color{darkgray} \textit{\VAR{proj.techStack|join(', ')}}}
    \vspace{4pt}
    \noindent
    {\large \color{darkgray} \VAR{proj.description}}
    \BLOCK{if proj.bullets}
    \begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in proj.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \BLOCK{endif}
    \vspace{6pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- EDUCATION ---
\BLOCK{if data.education}
\section{Education}
\BLOCK{for edu in data.education}
    \noindent
    \textbf{\Large \VAR{edu.institution}} \hfill {\color{darkgray} \VAR{edu.startDate} -- \VAR{edu.endDate}} \\
    {\large \VAR{edu.degree}} 
    \BLOCK{if edu.score} \quad \textbullet \quad GPA: \VAR{edu.score} \BLOCK{endif}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- SKILLS ---
\BLOCK{if data.skills}
\section{Skills}
\noindent
\VAR{data.skills|join(', ')}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Achievements}
\begin{itemize}[leftmargin=*, label={}, itemsep=4pt]
    \BLOCK{for ach in data.achievements}
    \item \large \color{darkgray} \VAR{ach}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=*, label={}, itemsep=4pt]
    \BLOCK{for cert in data.certifications}
    \item \large \color{darkgray} \VAR{cert}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- PUBLICATIONS ---
\BLOCK{if data.publications}
\section{Publications}
\BLOCK{for pub in data.publications}
    \noindent
    {\large \color{darkgray} \textbf{\VAR{pub.title}}. \textit{\VAR{pub.journal}}. \VAR{pub.date}.}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

\end{document}
