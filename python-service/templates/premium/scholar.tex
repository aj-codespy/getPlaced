%-------------------------
% Resume in Latex
% Scholar Template - Academic, Left-Aligned Headers, Bibliography Style
%------------------------

\documentclass[11pt,a4paper]{article}

\usepackage{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{xurl}

% Margins
\geometry{left=0.5in, right=0.5in, top=0.5in, bottom=0.5in}

\pagestyle{empty}
\urlstyle{same}
\setlength{\emergencystretch}{3em}
\sloppy
\hfuzz=3pt

% Section Titles: Left side prominent
\titleformat{\section}
  {\Large\bfseries\sffamily} 
  {} 
  {0em} 
  {} 
  [\vspace{-2pt}\hrule height 1pt]

\titlespacing{\section}{0pt}{10pt}{5pt}

\begin{document}

% --- HEADER (Left Aligned) ---
\noindent
{\large \textbf{\sffamily \parbox{\textwidth}{\raggedright \VAR{data.personalInfo.fullName}}}} \\
\vspace{4pt}
{\small \parbox{\textwidth}{\raggedright
\BLOCK{if data.personalInfo.email}\nolinkurl{\VAR{data.personalInfo.email}} \\ \BLOCK{endif}
\BLOCK{if data.personalInfo.phone}\VAR{data.personalInfo.phone} \\ \BLOCK{endif}
\BLOCK{if data.personalInfo.location}\VAR{data.personalInfo.location} \\ \BLOCK{endif}
\BLOCK{if data.personalInfo.displayLinks}
  \BLOCK{for link in data.personalInfo.displayLinks}\href{\VAR{link.url}}{\VAR{link.label}}\BLOCK{if not loop.last} \quad | \quad \BLOCK{endif}\BLOCK{endfor}
\BLOCK{endif}
}}

\vspace{8pt}

% --- SUMMARY ---
\BLOCK{if data.personalInfo.summary}
\noindent {\small \VAR{data.personalInfo.summary}}
\vspace{6pt}
\BLOCK{endif}

% --- EDUCATION ---
\BLOCK{if data.education}
\section{Education}
\BLOCK{for edu in data.education}
    \noindent
    \textbf{\VAR{edu.institution}} \hfill \VAR{edu.startDate} -- \VAR{edu.endDate} \\
    {\textit{\VAR{edu.degree}}} 
    \BLOCK{if edu.score} \hfill \textbf{GPA: \VAR{edu.score}} \BLOCK{endif}
    \BLOCK{if edu.coursework}
    \\ \small \textit{Relevant Coursework:} \VAR{edu.coursework}
    \BLOCK{endif}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- RESEARCH INTERESTS / SKILLS ---
\BLOCK{if data.skills}
\section{Research Interests \& Skills}
\noindent
\VAR{data.skills|join(', ')}
\BLOCK{endif}

% --- PUBLICATIONS ---
\BLOCK{if data.publications}
\section{Publications}
\begin{enumerate}[leftmargin=*, label={[\arabic*]}]
\BLOCK{for pub in data.publications}
    \item \VAR{pub.title}. \textit{\VAR{pub.journal}}. (\VAR{pub.date}).
\BLOCK{endfor}
\end{enumerate}
\BLOCK{endif}

% --- EXPERIENCE ---
\BLOCK{if data.experience}
\section{Research \& Work Experience}
\BLOCK{for exp in data.experience}
    \noindent
    \textbf{\VAR{exp.role}} \hfill \VAR{exp.startDate} -- \VAR{exp.endDate} \\
    {\VAR{exp.company}}
    \begin{itemize}[leftmargin=0.7em, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in exp.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \vspace{4pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- PROJECTS ---
\BLOCK{if data.projects}
\section{Technical Projects}
\BLOCK{for proj in data.projects}
    \noindent
    \textbf{\VAR{proj.title}} \hfill \textit{\VAR{proj.techStack|join(', ')}} \\
    \small \VAR{proj.description}
    \BLOCK{if proj.bullets}
    \begin{itemize}[leftmargin=0.7em, label={\textbullet}, itemsep=1pt, parsep=0pt, topsep=2pt]
        \BLOCK{for bullet in proj.bullets}
        \item \small \VAR{bullet}
        \BLOCK{endfor}
    \end{itemize}
    \BLOCK{endif}
    \vspace{3pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- ACHIEVEMENTS ---
\BLOCK{if data.achievements}
\section{Awards \& Honors}
\begin{itemize}[leftmargin=0.7em, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for ach in data.achievements}
    \item \small \VAR{ach}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

% --- CERTIFICATIONS ---
\BLOCK{if data.certifications}
\section{Certifications}
\begin{itemize}[leftmargin=0.7em, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for cert in data.certifications}
    \item \small \VAR{cert}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

\end{document}
